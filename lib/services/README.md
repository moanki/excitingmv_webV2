# Services

This folder will host server-side service modules for:

- partner approvals
- resort management
- protected resource access
- newsletter and registration capture
- admin AI import workflows
- chat inbox and realtime helpers

The first scaffold keeps these as planned extension points so the app structure is stable before implementation expands.
